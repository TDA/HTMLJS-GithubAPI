
var graph1 = {
  name: ".projects-bar-chart-svg",
  height: 500,
  width: 1000
};

drawBarGraph(jsonData1, graph1);

var graph2 = {
  name: ".loc-bar-chart-svg",
  height: 500,
  width: 1000
};

drawBarGraph(jsonData2, graph2);